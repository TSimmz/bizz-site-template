import {
  defineBlock,
  defineField,
} from '@storyblok/schema';

export const globalHeaderBlock = defineBlock({
  name: 'global_header',
  display_name: 'Header Global',
  is_root: false,
  is_nestable: true,
  description: '',
  internal_tag_ids: [
    '216527848693341',
  ],
  fields: [],
});
